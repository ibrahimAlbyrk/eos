{
  "name": "@eos/infra",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Infrastructure adapters for core/ ports. Every node:* + 3rd-party import in the daemon path lives here.",
  "main": "src/index.ts",
  "scripts": {
    "test": "node --no-warnings --experimental-strip-types --test src/__tests__/*.test.ts"
  },
  "dependencies": {
    "@eos/contracts": "file:../contracts",
    "@eos/core": "file:../core",
    "@modelcontextprotocol/sdk": "^1.18.0",
    "@vscode/tree-sitter-wasm": "^0.3.1",
    "chokidar": "^4.0.3",
    "web-tree-sitter": "^0.26.10",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
