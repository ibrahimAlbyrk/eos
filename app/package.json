{
  "name": "eos-electron",
  "productName": "Eos",
  "version": "0.0.0",
  "private": true,
  "description": "Eos desktop app (Electron). Serves app/ui/dist over a re-registered eos:// scheme and connects to the already-running local daemon. Builds/runs only inside this dir — never touches /Applications/Eos.app.",
  "main": ".forge-build/main.js",
  "author": "Eos",
  "license": "UNLICENSED",
  "scripts": {
    "bundle": "node esbuild.mjs",
    "build:ui": "npm --prefix ui run build",
    "ensure:ui": "test -f ui/dist/index.html || npm run build:ui",
    "typecheck": "tsc --noEmit",
    "start": "npm run ensure:ui && npm run bundle && electron-forge start",
    "package": "npm run bundle && electron-forge package",
    "make": "npm run bundle && electron-forge make",
    "clean": "rm -rf .forge-build out"
  },
  "devDependencies": {
    "@electron-forge/cli": "7.11.2",
    "@electron-forge/maker-zip": "7.11.2",
    "@types/node": "^24.0.0",
    "electron": "42.10.1",
    "esbuild": "0.28.2",
    "typescript": "^5.6.0"
  }
}
