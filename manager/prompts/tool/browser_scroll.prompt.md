---
description: "MCP tool — browser_scroll"
variables:
  - BROWSER_SNAPSHOT_TOOL
---

Scroll the page — `up`/`down` one viewport, `top`/`bottom` to the edge — or scroll a container by passing its `ref`. Content revealed by scrolling has no refs yet: take a fresh {{BROWSER_SNAPSHOT_TOOL}} before acting on it.
