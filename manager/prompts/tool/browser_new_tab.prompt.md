---
description: "MCP tool — browser_new_tab"
---

Open a new tab (optionally at `url`) and return `{ tabId }` — launches the browser if it is not running yet. Use it when no tabs are open, or to work on your own page without disturbing a tab the operator is watching. The agent origin allowlist applies to `url` like any navigation.
