---
description: "MCP tool — browser_type"
variables:
  - BROWSER_FILL_FORM_TOOL
---

Type text into one element by its `@eN` ref; `submit:true` presses Enter after. For several fields on the same form, {{BROWSER_FILL_FORM_TOOL}} in one call beats N separate type calls.
