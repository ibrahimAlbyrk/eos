---
description: "MCP tool — browser_get"
---

Read one value without a snapshot: `what` = "url" | "title" (page-level) or "text" | "value" (element-level, requires `ref`). The cheapest read there is — prefer it for verification ("did the URL change?", "what does the field say now?").
