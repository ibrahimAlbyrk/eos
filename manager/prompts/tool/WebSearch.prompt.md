---
description: "Built-in tool — WebSearch"
---
Searches the web for a query.

Note: web search is not configured on this lane — invoking this tool returns a clear error explaining that no search provider is available (a v1 limitation). To retrieve content from a known URL, use the WebFetch tool instead. The tool is present so the permission system can govern it and a provider can be enabled later without changing the surface.
