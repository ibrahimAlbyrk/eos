---
description: "Built-in tool — LS"
---
Lists the files and directories within a given path.

Usage:
- The `path` parameter may be an absolute path or a path relative to the worker's working directory.
- Directories are listed first and shown with a trailing slash; entries are sorted by name.
- To find files by name pattern use the Glob tool, and to search file contents use the Grep tool.
