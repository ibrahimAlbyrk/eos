---
description: "MCP tool — browser_close_tab"
variables:
  - BROWSER_TABS_TOOL
---

Close a tab. Omit `tabId` and the FIRST open tab closes — when several tabs are open, list them with {{BROWSER_TABS_TOOL}} and pass the id explicitly; the operator may be looking at one of them.
